{
  "name": "home-ops-bootstrap",
  "version": "1.0.0",
  "private": true,
  "description": "Infrastructure bootstrapping scripts for Proxmox home lab",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "bash scripts/lint.sh",
    "format": "bash scripts/lint.sh --fix",
    "lint:doctor": "bash scripts/lint-doctor.sh",
    "setup": "bash scripts/setup.sh"
  },
  "devDependencies": {
    "markdownlint-cli2": "0.22.1",
    "prettier": "3.8.3"
  }
}
